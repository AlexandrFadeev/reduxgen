import Foundation

struct @^Reducer: ReducerType {
    
    typealias State = @^State
    
    func reduce(result: ResultType, state: @^State) -> @^State {
        
        guard let @&Result = result as? @^Result else { return state }
        
//        switch accountResult {
//        case .loading: return AccountState(OneTimeEvent(true))
//        }
        #warning("Remove this line after implement 'switch-case'")
        return state
    }
}
